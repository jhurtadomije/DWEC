
//Pide numeros al usuario hasta que introduzca un 0

let numero = parseInt(prompt("introduce un número (0 para salir):"));

    while (numero !== 0) {
        numero = parseInt(prompt("introduce un número (0 para salir):"));
        }

        document.write("Fin del Programa");